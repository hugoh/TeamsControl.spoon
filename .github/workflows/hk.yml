name: hk

permissions:
  contents: read
  packages: read
  statuses: write

on: # yamllint disable-line rule:truthy
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    uses: hugoh/gh-workflows/.github/workflows/hk.yml@5aa8b976ce4126832a56a36c81c08d01b2b13073 # v1.37.2
    permissions:
      contents: read
      packages: read
      statuses: write
